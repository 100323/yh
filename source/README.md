# Legacy Reference Sources

`source/` stores older protocol or reverse-engineering reference files.

- It is **not** part of the current frontend/backend runtime.
- Treat files here as research material, not as the source of truth for active behavior.
- Prefer `frontend/src/` and `backend/src/` when implementing or fixing current features.
