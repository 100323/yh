# TaskIntentDraft

目标：让正常 cron 已触发但尚未执行的错峰任务在后端重启后自动恢复，并使漏做/恢复在前端可见。

范围：普通后端调度器、SQLite 槽位账本、日志 API、执行日志与调度观测页面。

非目标：不提高并发、不修改任务业务步骤、不自动回放已开始执行的任务、不处理业务提示类异常、不修改批量调度器。

基线：2026-07-18 `00:01` 任务在 `00:06` PM2 重启后出现无 marker、无日志缺口；内存 `pendingAccountTaskBatches` 是队列 owner。
